node_modules
dist
dist-ssr
dev-dist
coverage
*.local
.DS_Store

# raw pay/paystub source workbooks — never committed, see docs/BUILD_PLAN.md §6
data/source/
*.xlsx
*.tsbuildinfo

# font binaries for scripts/generate-icons.cjs — easy to re-fetch, no reason
# to carry font files in repo history (see that script's header comment)
scripts/.icon-fonts/
